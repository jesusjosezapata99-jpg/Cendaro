{
  "extends": "../../tooling/typescript/base.json",
  "compilerOptions": {
    "lib": ["ES2024", "dom", "dom.iterable"],
    "jsx": "preserve",
    "types": ["react", "react-dom", "node"],
    "paths": {
      "~/*": ["./src/*"]
    }
  },
  "include": ["src", "next-env.d.ts", ".next/types/**/*.ts", "*.ts", "*.js"],
  // Exclude .next/dev to avoid Turbopack dev server cache race condition; production .next/types is validated via build
  "exclude": ["node_modules", ".next/dev"]
}
