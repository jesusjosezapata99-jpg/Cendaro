import DeliveryNotesClient from "./client";

/**
 * Delivery Notes — Server Component
 * PRD §16: Notas de entrega para despacho
 */
export default function DeliveryNotesPage() {
  return <DeliveryNotesClient />;
}
