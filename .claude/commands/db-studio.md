Open Drizzle Studio to inspect the database:

```bash
pnpm db:studio
```

This runs `turbo -F @cendaro/db studio` which opens the Drizzle Studio web interface connected to the Supabase PostgreSQL database.